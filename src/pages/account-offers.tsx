// This file is no longer used since the subgraph is disabled.
// Offers are now created and found using direct on-chain lookups via the
// CreateOfferDialog and FindOfferDialog components.
export default function AccountOffers() {
  return null;
}
